"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const { Router } = require("express");
const tecnicasController = require("./tecnicas.controller");
const { validate } = require("../../middlewares/validate");
const { authenticate, authorize } = require("../../middlewares/auth");
const { tecnicaPayloadSchema, tecnicaPatchSchema } = require("./tecnicas.schemas");
const { idParamSchema } = require("../../schemas/params.schemas");
const tecnicasRouter = Router();
tecnicasRouter.get("/", tecnicasController.list);
tecnicasRouter.get("/:id", validate(idParamSchema, "params"), tecnicasController.getById);
tecnicasRouter.post("/", authenticate, authorize(["admin"]), validate(tecnicaPayloadSchema), tecnicasController.create);
tecnicasRouter.put("/:id", authenticate, authorize(["admin"]), validate(idParamSchema, "params"), validate(tecnicaPayloadSchema), tecnicasController.patch);
tecnicasRouter.patch("/:id", authenticate, authorize(["admin"]), validate(idParamSchema, "params"), validate(tecnicaPatchSchema), tecnicasController.patch);
tecnicasRouter.delete("/:id", authenticate, authorize(["admin"]), validate(idParamSchema, "params"), tecnicasController.remove);
module.exports = { tecnicasRouter };
